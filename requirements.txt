# =============================================================================
# Корпоративный ИИ-Ассистент (On-Premise RAG) — v4
# Зафиксированные версии (27.06.2026) — v4 = v3 + merge с petrel-rag-assistant
# =============================================================================

# --- RAG framework ---
llama-index==0.11.20
llama-index-vector-stores-qdrant==0.3.10
llama-index-embeddings-huggingface==0.3.1
qdrant-client==1.12.1

# --- LLM client (vLLM is OpenAI-compatible) ---
openai==1.55.0

# --- Embeddings & reranker ---
sentence-transformers==3.3.1
FlagEmbedding==1.3.2
transformers==4.46.3
torch==2.4.0
torchvision==0.19.0

# --- Document parsing ---
pymupdf==1.24.14
python-docx==1.1.2
docling==2.15.0
pymupdf4llm==0.0.17

# --- Backend & UI ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20
gradio==5.9.1
httpx==0.27.2

# --- Sparse retrieval (BM25 for hybrid) ---
rank-bm25==0.2.2

# --- Testing (v4: pytest с заглушками LLM, по образцу petrel) ---
pytest==8.3.4
pytest-asyncio==0.25.0

# --- v5: Prometheus monitoring ---
prometheus-client==0.21.1

# --- Utilities ---
python-dotenv==1.0.1
numpy==1.26.4
langdetect==1.0.9
hf-transfer==0.1.8

# ВАЖНО: RAGAS УБРАН в v4 — он по умолчанию обращается к OpenAI, что нарушает
# On-Premise ТЗ. Groundedness теперь считается локальным LLM-judge (см. app/llm.py).
